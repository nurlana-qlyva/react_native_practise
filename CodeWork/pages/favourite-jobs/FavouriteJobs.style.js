import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: { padding: 20 },
  card_container: {
    backgroundColor: "#fff",
    padding: 10,
    marginBottom: 10,
    borderRadius: 10,
  }

});
